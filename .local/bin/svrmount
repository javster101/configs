#!/usr/bin/env bash

server="tower.local"

while getopts s: flag
do
    case "${flag}" in
        s) server=${OPTARG};;
    esac
done

mount -t cifs //$server/$1 $2 -o credentials=/etc/samba/credentials/javier,iocharset=utf8,uid=1000,gid=1000,forceuid,forcegid,rw
